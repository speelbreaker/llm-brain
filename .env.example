# Deribit Testnet Credentials
# Get these from https://test.deribit.com -> Account -> API
DERIBIT_CLIENT_ID=your_client_id_here
DERIBIT_CLIENT_SECRET=your_client_secret_here

# Agent Configuration
DRY_RUN=true
LLM_ENABLED=false
LOOP_INTERVAL_SEC=300

# Risk Limits
MAX_MARGIN_USED_PCT=80
MAX_NET_DELTA_ABS=5.0

# Strategy Parameters
IVRV_MIN=1.0
DELTA_MIN=0.10
DELTA_MAX=0.35
DTE_MIN=1
DTE_MAX=14
PREMIUM_MIN_USD=50

# LLM Settings (if LLM_ENABLED=true)
LLM_MODEL_NAME=gpt-4.1-mini
